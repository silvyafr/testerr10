body {
    font-family: Arial, sans-serif;
    background-color: #87CEFA;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.input-form, .catalog {
    background-color: #ffffff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    width: 300px;
    text-align: center;
}

.catalog {
    display: flex;
    flex-direction: column;
}

.product-item {
    border-bottom: 1px solid #ccc;
    padding: 10px 0;
}

.product-item:last-child {
    border-bottom: none; /* Menghapus garis bawah pada produk terakhir */
}

.product-item h3 {
    margin: 0; /* Menghapus margin default */
}

.product-item p {
    margin: 5px 0; /* Menambahkan margin pada paragraf */
}

input {
    width: calc(100% - 22px); /* Menghitung lebar dengan padding dan border */
    padding: 10px;
    margin: 10px auto; /* Mengatur margin otomatis untuk pusat */
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 14px;
}

button {
    width: calc(50% - 12px); /* Menghitung lebar dengan margin */
    padding: 10px;
    margin: 10px 1%;
    border: none;
    border-radius: 4px;
    font-size: 14px;
    cursor: pointer;
    background-color: #FFA07A;
    color: white;
    transition: background-color 0.3s, transform 0.3s; /* Tambahkan transisi untuk transform */
}

button:hover {
    background-color: #8B0000; /* Warna saat hover */
    transform: translateY(-3px); /* Efek angkat saat hover */
}

button#cancel {
    background-color: #6c757d; /* Warna tombol cancel */
}

button#cancel:hover {
    background-color: #5a6268; /* Warna saat hover untuk cancel */
}
